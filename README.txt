This directory is GENERATED by `scripts/deploy_site.py`.
Do not hand-edit — your changes will be overwritten on the next deploy.

Source files live in:
  - ../verb-drill/         (index.html and data.jsonl)
  - ../number-drill/       (index.html and data.jsonl)
  - ../little-words/       (index.html and data.jsonl)
  - ../poems/              (one shared index.html + a .json per poem;
                            the player loads ?poem=<slug>)
  - ../audio/              (shared audio cache; site/audio/ holds only the
                            mp3s referenced by the tools and poems)

Published to a separate public repo via:
  git subtree push --prefix=site <public-remote> main
